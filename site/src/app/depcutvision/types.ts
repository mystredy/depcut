import type { LucideIcon } from "lucide-react";

export type Stat = {
  eyebrow: string;
  label: string;
  value: string;
};

export type Feature = {
  description: string;
  icon: LucideIcon;
  title: string;
};
