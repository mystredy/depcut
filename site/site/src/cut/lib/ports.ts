/**
 * The engine's default loopback port. Shared by the engine (bind address and
 * the mcp-proxy base URL) and the client (probe list) so the three of them
 * cannot drift.
 */
export const DEFAULT_ENGINE_PORT = 41417;
